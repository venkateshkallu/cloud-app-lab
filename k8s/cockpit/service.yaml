apiVersion: v1
kind: Service

metadata:
  name: cockpit
  namespace: credentialing

spec:
  selector:
    app: cockpit

  ports:
    - protocol: TCP
      port: 8004
      targetPort: 8004

  type: ClusterIP